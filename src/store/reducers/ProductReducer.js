const ProductReducer = (state = {} , action) => {
    switch (action.type) {
        case ('PRODUCT_ADDED') : {
            return state
        }
        case ('PRODUCT_ADDED_ERR') : {
            return state
        }
    }
}